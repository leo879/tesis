<table width="800" border="0" align="center">
    <thead>
    <tr>
      <th>Categoría</th>
      <th colspan="3"></th>
    </tr>
    </thead>
    <tbody align="center">
    <% obj.each do |categories| %>

        <td><%= categories.nombre %></td>
        <td><%= link_to image_tag('ver.png', title: 'Ver'), categories %></td>
         <%if logged_in? &&  (current_user == categories.user || current_user.admin?) %>
        <td><%= link_to image_tag('edit.png', title: 'Editar'), edit_category_path(categories) %></td>
        <td> <td><%= link_to image_tag('eliminar.png', title: 'Eliminar'), categories, method: :delete, data: { confirm: 'Esta seguro de eliminar?' } %></td>
        <% end %>
        <tr></tr>
    <% end %>
    </tbody>
  </table>
